version: "2"
run:
  timeout: 5m
  go: "1.26"
linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - unconvert
    - prealloc
  disable:
    - errcheck
  settings:
    gocritic:
      enabled-checks:
        - ruleguard
